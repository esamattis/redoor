# Flaky tests

- `ui/e2e/server-logs.spec.ts > Server logs > navigates from the menu and shows the latest history in order` did not find the connected-agent link on the first `pn playwright` run, then all 134 Playwright cases completed successfully on retry (2026-08-17).
- `ui/e2e/transfer-links.spec.ts > Transfer Path Links > should navigate to browser view from transfer path link` failed during `pn test` after the Playwright server panicked with `JoinHandle polled after completion`; leftover processes then held port 31533 until they were terminated (2026-08-16).
- `pn playwright` failed because a leftover `redoor` process still held port 31533 after `pn test`, then all 113 Playwright tests passed after terminating that process (2026-08-16).
- `tests/watchdog.test.ts > Watchdog supervisor > intentionally shuts down and can restart a managed agent` failed because the old PID was briefly still observable, then passed on an immediate targeted retry (2026-08-09).
- `tests/watchdog.test.ts` stale-WebSocket restart and managed lifecycle cases timed out while the shared test binary was being rebuilt concurrently, then all 86 Vitest tests passed when rerun without a concurrent build (2026-08-09).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` timed out waiting for Terminal 2 to become selected, then all 48 Playwright tests passed on the immediate rerun (2026-08-09).
- `pn integration-test` initially failed because a process left by an interrupted test run still held the shared test port, then all 88 Vitest tests passed after terminating the stale `redoor` process (2026-08-09).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 52 Playwright tests passed on the immediate rerun (2026-08-09).
- `pn integration-test` failed because an interrupted run left a `redoor` server bound to port 36203, then all 90 Vitest tests and the full `pn test` suite passed after removing the stale test process (2026-08-09).
- `watchdog::tests::connection_generation_clears_issue_and_ignores_stale_disconnect` observed `Starting` instead of `Connected` during `pn test`, then passed on an immediate targeted retry (2026-08-09).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 54 Playwright tests passed on the immediate rerun (2026-08-09).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 54 Playwright tests passed on the immediate rerun (2026-08-10).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 54 Playwright tests passed on the immediate rerun (2026-08-10).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 54 Playwright tests passed on the immediate rerun (2026-08-10).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out during relay daemon fix validation, then all 54 Playwright tests passed on the immediate rerun (2026-08-10).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then passed on targeted retry and full Playwright suite (2026-08-11).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 60 Playwright tests passed on the immediate rerun (2026-08-12).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` timed out because overlay chrome repeatedly intercepted a directory-link click during `pn test`, then passed on an immediate targeted retry (2026-08-16).
- `ui/e2e/agent-management.spec.ts > Agent management > switches an existing agent from password auth to key auth` failed during `pn test`, then all 111 Playwright tests passed on the immediate rerun (2026-08-15).
- `ui/e2e/agent-view-navigation.spec.ts > Agent view navigation > switches connected agent views and selects an agent from the mobile drawer` and `ui/e2e/browser-navigation.spec.ts > File Browser Navigation > should filter directory entries and navigate to the first match` failed during `pn test`, then all 99 runnable Playwright tests passed on the immediate rerun (2026-08-15).
- `pn playwright` lost its test server after the restart workflow, causing nine unrelated specs to fail with connection errors; all 104 Playwright tests passed in the subsequent full `pn test` run (2026-08-15).
- `ui/e2e/file-operations.spec.ts > File Operations > should upload files from directory view` again timed out during `pn test` and failed repeatedly in isolation because route refreshes raced after successful PUTs; the test now waits for both upload responses and reloads before checking persisted directory contents (2026-08-13).
- `ui/e2e/file-operations.spec.ts > File Operations > should upload files from directory view` twice timed out waiting for `uploaded-a.txt` in the full Playwright sequence, then passed on an immediate isolated retry (2026-08-13).
- `ui/e2e/file-operations.spec.ts > File Operations > should upload files from directory view` again timed out waiting for `uploaded-a.txt` during search feature validation, then passed on an immediate isolated retry; the search-specific 21-test browser suite also passed (2026-08-13).
- `ui/e2e/file-operations.spec.ts > File Operations > should upload files from directory view` timed out waiting for an uploaded file to appear during `pn test`, then all 74 Playwright tests passed on the immediate rerun (2026-08-13).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during relay watchdog validation, then all 60 Playwright tests passed on the immediate rerun (2026-08-12).
- `ui/e2e/terminal.spec.ts > Terminal panel lifecycle > keeps independent terminal tabs in their captured directories` again timed out waiting for Terminal 2 to become selected during `pn test`, then all 60 Playwright tests passed on the immediate rerun (2026-08-12).
