# Plan: Extract Request ID Creation to Method

## Overview
Refactor request ID creation by extracting duplicate code to a `next_id()` method and evaluate using `u64` instead of `String`.

## Current State
- Request IDs are created in 2 places with identical code: `format!("req_{}", state.next_request_id); state.next_request_id += 1;`
- Type: `String` with format `"req_{number}"`
- Counter: `u64` in `RouterState.next_request_id`
- Used for matching REST API responses via HashMap lookup
- Passed through WebSocket messages but not used for WebSocket response matching

## Files to Modify
- `/Users/esamatti/code/redoor/src/actors/router.rs` - Main router implementation
- `/Users/esamatti/code/redoor/src/types.rs` - Message enum (if changing to u64)
- `/Users/esamatti/code/redoor/src/bin/redoor-agent.rs` - Agent binary (if changing to u64)
- `/Users/esamatti/code/redoor/src/actors/session.rs` - Session actor (minor updates if changing to u64)

## Implementation Approach

### Change request_id type from String to u64

**Benefits:**
- More efficient (no string allocation)
- Simpler code
- Better for serialization
- Cleaner HashMap keys

**Changes:**

1. **Update `Message` enum** in `/Users/esamatti/code/redoor/src/types.rs`:
   - Change `Message::Command.request_id` from `String` to `u64`
   - Change `Message::CommandResponse.request_id` from `String` to `u64`

2. **Update `RouterMsg` enum** in `/Users/esamatti/code/redoor/src/actors/router.rs`:
   - Change `RouterMsg::RouteResponse.request_id` from `String` to `u64`
   - Update `rest_pending_responses` HashMap key from `String` to `u64`

3. **Add `next_id()` method** to `RouterState`:
   ```rust
   impl RouterState {
       fn next_id(&mut self) -> u64 {
           let id = self.next_request_id;
           self.next_request_id += 1;
           id
       }
   }
   ```

4. **Replace request ID creation** in two locations (lines 177-178 and 275-276):
   - `let request_id = state.next_id();`

5. **Update agent binary** in `/Users/esamatti/code/redoor/src/bin/redoor-agent.rs`:
   - Remove `.clone()` calls on request_id (no longer needed)
   - Update any string-specific handling

6. **Update session actor** in `/Users/esamatti/code/redoor/src/actors/session.rs`:
   - Remove `.clone()` calls on request_id (no longer needed)

7. **Update logs** to show numeric request IDs instead of "req_X" format

## Verification

1. Run `./scripts/build-and-test` to ensure all tests pass
2. Verify request IDs are generated correctly as u64
3. Test REST API command execution to ensure request ID matching works correctly with u64 keys
4. Verify WebSocket protocol changes work - agent should serialize/deserialize u64 request IDs correctly
5. Check logs to ensure request IDs are displayed correctly
